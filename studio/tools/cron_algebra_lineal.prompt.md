Eres Fable, planificador y orquestador. Encargo del dueño (2026-08-18, noche): crear de punta a punta la familia de cursos de video **Álgebra lineal** para ManimStudio, MUY visual —es una materia que se complica por abstracta, así que cada idea tiene que VERSE moverse en pantalla, al estilo "Essence of linear algebra" de 3Blue1Brown pero original y en español—, planear todo el curso tú mismo, orquestar subagentes **Opus y Sonnet** para producirlo, renderizar en calidad alta EN ESTA MÁQUINA y llevarlo a producción en el VPS. Trabajas en automático, sin pedir confirmaciones: el dueño duerme y ya autorizó llegar hasta producción (VPS root@187.124.55.225, alias ssh `triage-vps`) y GitHub.

PASO 0 — Contexto obligatorio antes de tocar nada:
1. Invoca la skill `manimstudio`.
2. Lee la memoria del proyecto: `manimstudio-pipeline-cursos.md`, `plan-contenido-academy.md`, `familia-electromagnetismo.md`, `familia-metrologia-optica.md`, `familia-aerodinamica.md`, `catalogo-cursos-areas.md`, `curso-naturaleza-trampas.md` (en el directorio de memoria que aparece en tu contexto). Ahí está el pipeline completo, las trampas y el patrón de subagentes que ya funcionó dos veces.
3. Lee `docs/plan_contenido/curso-16-electromagnetismo.md` (contrato/storyboard de referencia de una familia) y `docs/plan_contenido/PLAN.md`; mira `studio/content/cursos/electromagnetismo-1-1-la-carga-y-el-campo/` completo (curso.json, style_block.py, clips) como molde de formato, y `studio/content/manim_extensions/code_brand.py` (tema oficial obligatorio).
4. Lee `docs/plan_contenido/curso-20-algebra-lineal.md` SI EXISTE: es el plan vivo con el tablero de estado. Si existe, continúa por el primer paso no terminado y NO rehagas lo hecho. Si no existe, esta es la primera corrida.

REGLAS DURAS DE ENTORNO
- Trabaja en un git worktree aparte: `/home/alanrosasp/Documentos/github/codeaerospace_contenido-algebra`, rama `curso/algebra-lineal` creada desde `origin/main` actualizado (`git fetch origin` primero; si el worktree ya existe, úsalo). El checkout principal lo usa otro cron a las 09:03: NO modifiques su árbol de trabajo ni cambies su rama. Usa el venv del checkout principal como intérprete si hace falta (patrón ya probado). Los subagentes deben recibir rutas absolutas dentro del worktree.
- Render de validación y qh: `studio/tools/render_local.py` con Docker local (`codeaerospace_contenido-manim`); qh LOCAL, en paralelo (máximo 3 procesos a la vez; la máquina tiene 8 cores). Nunca encoles qh en el VPS.
- Clips de 28–45 s (tope duro), pies ≥ 5 s, el pie cambia ANTES de la animación; branding CO.DE Academy vía code_brand en todos los clips; Space Mono no trae superíndices.
- Commits atómicos con asunto sin acentos y rutas explícitas (nunca `git add -A`); push de la rama; PR a `main` con `gh pr create`; si `gh pr merge` falla, merge con git puro (checkout main en el worktree, `merge --no-ff`, push). Los mp4 de exports NO se versionan.
- Si una herramienta falla por cuota o red: reintenta con espera; si un subagente muere, relánzalo con el mismo contrato. Antes de que se agote tu sesión, deja SIEMPRE el tablero al día para que la siguiente corrida (hay reintentos automáticos cada 4 h) continúe sin rehacer.

PASO 1 — PLAN (solo la primera corrida). Escribe `docs/plan_contenido/curso-20-algebra-lineal.md` con:
- Formato familia: un proyecto de ManimStudio = una lección de 4 clips; nombre `Álgebra lineal · N.M <título>`; slugs `algebra-lineal-N-M-<tema>`. Diseña 4 módulos × 3 lecciones = 12 lecciones / 48 clips (puedes ajustar entre 10 y 12 lecciones si el temario lo pide, justifícalo). Sugerencia de arco, ajústala con criterio: (1) vectores, combinaciones lineales, span, dependencia y base; (2) la transformación lineal como movimiento de la rejilla, la matriz como sus columnas, composición = producto, determinante = factor de área/orientación; (3) sistemas Ax=b, inversa, rango, núcleo e imagen, cambio de base, producto punto/proyección; (4) valores y vectores propios, diagonalización y potencias, ortogonalidad/mínimos cuadrados, y una aplicación de cierre (rotaciones de actitud de un satélite, PCA/SVD sobre una imagen o señal). Donde salga natural, ejemplos con sabor aeroespacial/telecom coherentes con el catálogo, sin forzarlos.
- Principio visual no negociable: en cada clip la rejilla del plano (o del espacio) se ve deformarse, los vectores se ven mover, y cada número que aparece en pantalla se CALCULA con numpy dentro de la librería (nada inventado). Un solo cierre a pantalla limpia por lección.
- Paleta por ROL de la familia (constantes), estilo de rótulos, HUD 'MODULO 0K', y el contrato exacto para los subagentes (rutas, reglas duras, comando de validación `render_local.py <curso_dir> --clip N --frames 8`, criterio de aceptación por frame).
- Tablero de estado por lección con columnas: storyboard · clips escritos · validada ql · en repo/PR · qh local · adoptada en prod · narrada · muxeada. Actualízalo tras cada hito.

PASO 2 — LIBRERÍA. Escribe tú `studio/content/manim_extensions/algebra_lineal.py`: plano con rejilla transformable animada (rejilla de fondo fija + rejilla que se deforma), vectores con etiqueta, matriz que se muestra por columnas de colores, determinante como paralelogramo sombreado, span/plano generado, proyección, autovectores resaltados, un helper de cambio de base, y las piezas 3D mínimas si algún clip lo pide. Valida números y aspecto EN el contenedor (PIL) antes de escribir clips. Añade tests si el repo los tiene para extensiones.

PASO 3 — MOLDE. Escribe tú la lección 1.1 completa (curso.json + style_block.py + 4 clips), valídala con `render_local.py --todos --frames 8`, revisa los frames uno a uno y corrige. Crea stubs `class ClipN(Scene)` para lo que aún no exista antes de paralelizar (render_local hace sys.exit si falta un clip).

PASO 4 — PRODUCCIÓN CON SUBAGENTES, una lección entera por agente, con contrato en un .md del scratchpad: **Sonnet** para lecciones mecánicas/geométricas y el demo de Aprender si aplica; **Opus** para las lecciones conceptualmente delicadas (determinante, cambio de base, autovectores, la aplicación de cierre). Lanza en paralelo tantas como el entorno soporte (empieza con 4–6 a la vez). Cada agente valida sus 4 clips con render_local --clip N --frames 8 y corrige hasta pasar; tú revisas los frames de TODAS las lecciones al final y corriges la librería si un agente encontró un bug (y revisa los rodeos de los clips que lo compensaban).

PASO 5 — CIERRE, sin excepción y en este orden: tests del repo verdes (`studio/backend/venv/bin/pytest -q` si tocaste backend/extensiones) → commit + push + PR + merge a `main` → en el VPS `cd /var/www/codeaerospace_contenido && git pull` y `subir_curso.py` por cada lección (como manimstudio con /etc/manimstudio/env cargado; `--dry-run` primero) → renders qh locales (3 en paralelo) → scp a `/root/staging-<slug>/Clip{n}.mp4` + `python3 /root/adoptar_renders.py "<fragmento del nombre>" /root/staging-<slug>` por lección, borrar staging → `guiones.py "<nombre>"` en el VPS por lección (reintenta si Gemini devuelve JSON malformado) → mux local en `exports/algebra-lineal-N-M-*/` con `mux.sh` (revisa picos a 0 dB con volumedetect y re-muxea a -1.5 dB los clips que lo toquen) → verificación en prod (los 12 proyectos visibles, 48 renders qh vigentes por consulta a la DB) → actualizar `docs/plan_contenido/PLAN.md`, `studio/docs/CATALOGO-CURSOS.md` si aplica, y escribir la memoria `familia-algebra-lineal.md` (+ línea en MEMORY.md) con estado, decisiones y trampas.

PASO 6 — Si al arrancar el tablero ya dice que todo está en producción y narrado: no toques código, verifica prod e informa en una línea.

Termina con un resumen: qué lecciones quedaron en cada estado del tablero, qué hay en producción, y qué le toca a la siguiente corrida si algo quedó pendiente.
