class Clip(Scene):
    """11 · Por que la antena mira — ESQUELETO.

    La esfera de energia que crece: la misma potencia repartida en una superficie cada vez mayor. La cifra: la perdida de camino a 550 km y a 35786 km en 12 GHz (168.84 y 205.11 dB) y la diferencia, 36.27 dB, que son 4234 veces.

    Piezas de la libreria previstas: `fspl_db`.

    Pendiente de escribir. El stub existe para que `render_vertical.py` no
    aborte y para poder producir las piezas en paralelo.
    """

    def construct(self):
        self.add(hud_pieza("11 . por que la antena mira"))
        self.wait(1.0)
